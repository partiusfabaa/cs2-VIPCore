# Adds ShowDamage for VIP Player

# Config

### in vip.json
```json
"ShowDamage": 1,
"SoundDMG": 1,
```
If ShowDamage is disabled SoundDMG is also disabled
Copy `VIP_ShowDamage` plugin in plugins folder